diff --git a/node_modules/oclif/lib/commands/promote.js b/node_modules/oclif/lib/commands/promote.js
index d89b9c6..9c3f4a5 100644
--- a/node_modules/oclif/lib/commands/promote.js
+++ b/node_modules/oclif/lib/commands/promote.js
@@ -41,6 +41,7 @@ class Promote extends core_1.Command {
             // eslint-disable-next-line no-await-in-loop
             await aws_1.default.s3.copyObject({
                 Bucket: s3Config.bucket,
+                ACL: s3Config.acl || 'public-read',
                 CopySource: copySource,
                 Key: key,
                 CacheControl: maxAge,
@@ -60,6 +61,7 @@ class Promote extends core_1.Command {
             // eslint-disable-next-line no-await-in-loop
             await aws_1.default.s3.copyObject({
                 Bucket: s3Config.bucket,
+                ACL: s3Config.acl || 'public-read',
                 CopySource: versionedTarGzKey,
                 Key: unversionedTarGzKey,
                 CacheControl: maxAge,
@@ -83,6 +85,7 @@ class Promote extends core_1.Command {
                 // eslint-disable-next-line no-await-in-loop
                 await aws_1.default.s3.copyObject({
                     Bucket: s3Config.bucket,
+                    ACL: s3Config.acl || 'public-read',
                     CopySource: versionedTarXzKey,
                     Key: unversionedTarXzKey,
                     CacheControl: maxAge,
@@ -106,6 +109,7 @@ class Promote extends core_1.Command {
                 // eslint-disable-next-line no-await-in-loop
                 await aws_1.default.s3.copyObject({
                     Bucket: s3Config.bucket,
+                    ACL: s3Config.acl || 'public-read',
                     CopySource: darwinCopySource,
                     Key: darwinKey,
                     CacheControl: maxAge,
@@ -129,6 +133,7 @@ class Promote extends core_1.Command {
                 // eslint-disable-next-line no-await-in-loop
                 await aws_1.default.s3.copyObject({
                     Bucket: s3Config.bucket,
+                    ACL: s3Config.acl || 'public-read',
                     CopySource: winCopySource,
                     Key: winKey,
                     CacheControl: maxAge,
@@ -158,6 +163,7 @@ class Promote extends core_1.Command {
                 // eslint-disable-next-line no-await-in-loop
                 await aws_1.default.s3.copyObject({
                     Bucket: s3Config.bucket,
+                    ACL: s3Config.acl || 'public-read',
                     CopySource: debCopySource,
                     Key: debKey,
                     CacheControl: maxAge,
diff --git a/node_modules/oclif/lib/version-indexes.js b/node_modules/oclif/lib/version-indexes.js
index 2410048..ca12848 100644
--- a/node_modules/oclif/lib/version-indexes.js
+++ b/node_modules/oclif/lib/version-indexes.js
@@ -64,6 +64,7 @@ const appendToIndex = async (input) => {
         Bucket: s3Config.bucket,
         Key: key,
         CacheControl: maxAge,
+        ACL: s3Config.acl || 'public-read',
     });
     // cleans up local fs
     await fs.remove(jsonFileName);
